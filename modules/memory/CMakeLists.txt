AUX_SOURCE_DIRECTORY(. ONVIF_MEMORY)

ADD_LIBRARY(onvif_memory SHARED)
SET_TARGET_PROPERTIES(onvif_memory PROPERTIES VERSION 0.0.2 SOVERSION 0)
TARGET_SOURCES(onvif_memory PRIVATE ${ONVIF_MEMORY})

INSTALL(TARGETS onvif_memory
		LIBRARY DESTINATION onvif/lib)